version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo "Installing dependencies..."
      - npm install  # Re-enable npm install for proper dependency installation
      
  build:
    commands:
      - echo "Building the application..."
      - npm run build || echo "No build script found, skipping..."
      - bad-command-here  # Intentionally trigger a failure

  post_build:
    commands:
      - echo "Build completed successfully!"
